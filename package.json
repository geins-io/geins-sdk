{
  "name": "geins",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "build": "npx turbo run build",
    "dev": "npx turbo run dev",
    "dev:nuxt": "npx turbo run dev --filter=nuxt-app -- --port 3000",
    "dev:next": "npx turbo run dev --filter=next-app -- --port 3111",
    "dev:svelte": "npx turbo run dev --filter=svelte-app -- --port 3112",
    "start:nuxt": "npx turbo run start --filter=nuxt-app",
    "build:nuxt": "npx turbo run build --filter=nuxt-app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.4"
  },
  "packageManager": "npm@10.8.1"
}