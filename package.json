{
  "name": "geins",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "build": "npx turbo run build",
    "dev": "npx turbo run dev",
    "dev:nuxt": "npx turbo run dev --filter=nuxt-app",
    "dev:next": "npx turbo run dev --filter=next-app",
    "dev:svelte": "npx turbo run dev --filter=svelte-app",
    "start:nuxt": "npx turbo run start --filter=nuxt-app",
    "build:nuxt": "npx turbo run build --filter=nuxt-app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.4"
  },
  "packageManager": "yarn@1.22.22"
}