/** Pre-generated image folders available on the Geins CDN. */
export const GEINS_IMAGE_FOLDER = {
  /** Original uploaded image (full resolution). */
  RAW: 'raw',
  /** 40×40 pixel small thumbnail. */
  THUMB_SM: '40x40',
  /** 100×100 pixel thumbnail. */
  THUMB: '100x100',
} as const;
