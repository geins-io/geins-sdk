schema: '../../../schema.graphql' # Path to the GraphQL schema
documents:
  # - '../core/src/graphql/**/*.gql'
  - '../crm/src/graphql/**/*.gql'
generates:
  src/generated/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
